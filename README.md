# ubuntu with open-jdk

## 👨🏻‍💻 Running Locally

```bash
# build
docker build -t ubuntu-jdk-17 .

# run
# sample-container : 컨테이너이름. 원하는 이름으로 변경하기.
docker run -td --name sample-container ubuntu-jdk-17 
```
